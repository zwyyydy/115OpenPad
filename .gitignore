# 工具链与 SDK（体积巨大，不入库）
tools/
android-sdk/

# 构建产物
build/
app/build/
.gradle/
local.properties
*.apk.idsig
*.apk

# 含敏感令牌的临时文件（token 注入脚本/二进制）
spb*.bin
fix_pb.py
.fresh_token

# 截图等临时产物
*.png
*.jpg

# IDE
.idea/
*.iml
.DS_Store

# 本地工作记录与内部笔记（不上库）
.workbuddy/
docs/

# AI 助手的工作目录（计划草稿等）
.zcode/

# 参考实现：别人的项目源码，只作本地对照
ref/

# adb / uiautomator 抓的界面 dump（都落在仓库根目录）
/*.xml

# 上传与播放测试用的素材（体积大，本地复现问题用）
test_upload_*.bin
test_video_*.mp4
test_*.bin
test_*.py

# 临时令牌/文件
.tmp_*
*.pb

# VR 素材与转换产物（体积大，属本地工作文件，不入库）
vr_source/
vr_out/

# 本地 API 笔记与工具配置（不入库）
115_api_docs.md
.zcodeignore
