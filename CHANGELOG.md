# 更新日志

## 0.4.0

### 新增

- **媒体库（海报墙）**：把网盘里的影视目录扫成本地索引 —— 海报墙 → 详情页（简介 / 演员 / 标签 / 剧照 / 技术参数），剧集自动分层成一张系列卡、点进去选集
  - 读同目录的 `.nfo`（47 个字段，含 `<fileinfo>` 技术参数）与 `poster / fanart / thumb / extrafanart / .actors` 侧挂素材；演员、标签可点，跨所有媒体库看同演员 / 同标签作品
  - 增量扫描按目录指纹判断（删除 / 改名 / 移入都能发现）、每库可设自动扫描间隔、扫描可停止；扫描记录（何时扫了哪个库、结果、新增影片）在媒体库页里看
  - 海报与简介落盘缓存，命中不再解析直链；缓存上限可设（含「不限制」）
  - 排序 9 档（可正反序）、筛选（年份 / 演员 / 类型·标签）、筛选旁可随机播放；搜索是全局的（片名 / 演员 / 标签）
  - 观影历史与续播（115 没有"列出看过的片"的接口，本机自己记）
  - 条目可「从媒体库移除」或「彻底删除」（连海报 / 剧照一起删）
- **实验室**：设置页新增「实验室」分组（以后的尝鲜功能都放这里），第一项是**视频滤镜** —— 8 个预设 + 亮度 / 对比度 / 饱和度 / 色温 / 磨皮 / 暗角可调，参数全局记住；选「原图」或关掉开关仍走原 TextureView 路径，零额外开销；VR 下不生效
- **播放器**：截图存相册 `Pictures/115OpenPad/`（带字幕、跟随画面旋转，VR 下截的是所见画面）；硬解 / 软解就地切换（控制排右下角，不用退出播放器）；VR 进片 / 切集 / 换模式时视角自动归位
- **启动首页**：设置里可选启动落在「文件」还是「媒体库」
- **快捷目录**：文件页侧栏收藏常用目录，点一下直达；多选条目可直接拖到侧栏移动
- **手机模式**：底部导航从 8 项收敛为 5 项（云下载 / 回收站进传输中心，新增「更多」收纳低频入口）
- **设置页**：改为分类导航（平板左分类 + 右内容，手机列表 → 二级页），条目列表驱动

### 修复

- 播放器双击快进偶发失效（三层手势检测器互相取消，改为单判定者状态机）；长按倍速微调、切软 / 硬解后手势失效
- 大文件切清晰度失败（预载只备够起播的量）；切清晰度不再弹进度条
- 搜索结果打不开（视频按扩展名补 `isv`、文件夹 `fc` 兼容数字类型）
- 剧集库只入库一集（分集 nfo 的 uniqueid 全一样，主键撞成一行）
- 两个媒体库互相串数据（多根查询的空参数把 `LIKE` 变成通配符）
- 本地删除影片后增量扫描找不回条目；元数据与视频文件名前缀不一致时聚成两张卡
- 手机模式排版：库卡文字被挤成竖排、海报墙顶栏被撑高
- 星标概率性报「coroutine scope left the composition」

### 变更

- **播放缓存改为本次播放内有效**：播放地址每次进都会换签名，上一会话的缓存不可能命中，因此退出播放器即清空（设置页的「清除视频缓存」随之移除）
- **图片浏览缓存**：画廊图片按文件标识落盘，关掉画廊再进、重启应用都直接命中，不再重下整张原图

### 已知限制

- BT 原生任务未实现；批量重命名「文本」方式替换所有出现位置
- 文件夹置顶只存本机；4K / 原画播放需要 115 会员

## 0.3.0

### 新增

- **大文件上传**：分片流式上传 + 断点续传（中断 / 暂停后接着传，重启也认）；同时最多 2 个，超出的排队
- **文件夹上传**：选目录递归上传，远端自动按结构建目录
- **批量重命名**：查找替换（七种选段方式，可叠加多规则）、插入内容、自动编号（阿拉伯 / 中文大小写 / 英文字母）、手动编辑（粘贴多行自动向下填充）
- **重命名任务队列**：顺序执行、限速可调、可暂停 / 取消 / 重试失败项；**关页面、退出应用都不中断**，下次打开接着跑；左侧导航新增「重命名」页看进度，也可直接选云盘目录建任务
- **VR 观看**：180°/360° 立体素材实时反投影为平面；竖屏可选视窗档位（满屏 / 3:4 / 1:1）压掉垂直方向的边缘拉伸
- **本地文件播放**：已下载的视频点一下用应用内播放器打开（长按交给其他应用）；下载的 VR180 也能反投影
- **可作为外部播放器**：其他应用「打开 / 分享视频」都能唤起
- **播放器**：新增「原盘」档位（直链播原始文件）；竖屏播放布局重写，横竖屏完全沉浸
- **图片浏览全屏化**、**文件页侧栏改为操作记录**（点击跳回原目录）、**多选一键全选**

### 修复

- VR 陀螺仪方向错误、视角操作与原有手势冲突
- 续播位置贴着片尾时起播会立刻结束（黑屏）
- 上传回调校验误判（10002）
- 图片顶部信息条不再铺 45% 黑底

### 变更

- 上传任务支持暂停 / 继续 / 取消 / 重试（暂停保留会话，重启后不自动恢复）
- VR 菜单从画面中部移到控制排顶部

### 已知限制

- 批量重命名：「文本」方式替换所有出现位置；同目录已有任务在跑时新建任务的预览可能不准
- 手机底部导航现有 7 项偏挤（平板是左侧导航栏，无影响）
- 文件夹置顶只存本机；BT 原生任务未实现

## 0.2.0

### 新增

- **文件夹置顶**：长按多选 → 底部图钉按钮，把常用文件夹在本目录内固定排到最前（可批量）；置顶只存本机，换设备不同步
- **目录列表缓存**：60 秒内来回进出目录不再重复请求；顺带修掉"返回上级时滚动位置错乱"
- **文本预览**：txt / py / md / js 等点开即预览，大文件按行分页，可切换编码
- **图片**：放大后右下角浮出半透明鸟瞰位置图；超大图处理阈值 15MB → 10MB
- **播放器**：HDR 片源转 SDR 输出、软件解码开关、字幕位置可调、缓冲中动效、新增 1.75x 倍速档
- **传输中心**：本机下载（含实时速度 / 剩余时间）与上传记录
- **外部联动**：`mypad://` / `pan115://` / `magnet:` 与系统「分享到」直接提交云下载

### 修复

- 文件排序完全失效（服务端 `custom_order` 默认值让 `o` / `asc` 全被忽略）
- 竖屏片旋转 90°/270° 后画面被横向压扁
- 下载直链一律 403（直链与申请时的 User-Agent 绑定）
- 播放器右上角规格标签不显示；单档片源的画质按钮不显示
- 播放列表抽屉每次展开都回到顶部
- 文件浏览切到"加载中 / 空目录 / 错误"后滚动位置丢失
- 大图被反复重复下载（缓存文件名用了带签名的 URL，实测白占 103MB）；新增 200MB 上限与自动淘汰，设置页可手动清除
- 移动 / 复制成功后没有任何提示

### 变更

- 移除文件对话框的「复制直链」按钮（改由传输中心承担，避免私密直链外泄）
- 外部唤起优先于剪贴板探测；离线任务保存目录选定后持久化

### 已知限制

- 文件夹置顶只存本机；大文件上传、BT 原生任务未实现

## 0.1.0

首个版本：登录、文件浏览、图片大图画廊（含超大图分块解码）、视频播放器、云下载、回收站、小文件上传、过滤规则、大屏适配。
