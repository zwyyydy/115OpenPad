# 更新日志

面向使用者的版本更新与修复汇总。技术实现细节见各条提交信息。

## 0.2.0

0.1.0 之后的 9 次提交汇总。

### 新增

**文件浏览**

- **文件夹置顶**：长按多选 → 底部图钉按钮，把常用文件夹在**它所在的目录**里固定排到最前，可一次置顶多个；这些文件夹已全部置顶时按钮转实心态，再点即整体取消。置顶项带图钉角标与淡蓝底，一眼可分，拖动时会跟随定位。115 开放平台没有置顶接口，所以置顶只保存在本机、换设备不同步。
- **目录列表本地缓存**：60 秒内来回进出目录**零请求**；超过 60 秒则先渲染缓存、再后台静默刷新。顺带修掉了"从子目录返回上级时滚动位置错乱"的老问题。115 接口有频控，这项改动明显降低了被限流的概率。
- **文本预览**：txt / py / md / js 等文本文件点开即预览，大文件按行分页，顶栏可切换编码。

**图片**

- **鸟瞰位置图**：放大后右下角浮出半透明小图 + 当前视口框，长图 / 巨图放大到 5x 时不再"我在图的哪一块"迷失；下拉关闭时跟着一起淡出。
- 超大图的处理阈值从 15MB 下调到 **10MB**，更多单反原图能走分块解码路径。

**播放器**

- **HDR 片源转 SDR 输出**（API 31+）：修掉 HDR 片源画面泛白成灰雾的问题。
- **「软件解码」设置**：遇上花屏 / 黑屏 / 画面撕裂 / 起播失败时，可切 CPU 软解绕过；个别没有软解的编码会自动回退硬解。
- **「字幕位置」设置**：整层字幕按画面高度百分比上移，避开官方字幕自带的行位置。
- **播放缓冲动效**：网络卡顿时画面正中显示「缓冲中…」胶囊（延迟 400ms 出现，避免细微抖动闪一下），缓冲结束即淡出。
- 倍速档位新增 **1.75x**。
- 播放器状态行（时长 / 画质 / 缓存 / 解码方式）挪到进度条上方 —— 原先在根处会被盖住。
- **画质按钮始终显示**：720p 以下片源通常只有单档，原先按"档位数 > 1"判断会导致按钮不显示、用户无从得知当前画质。

**传输与外部联动**

- **传输中心**（导航栏「云下载」之后）：
  - 下载页签查系统 DownloadManager（只显示本应用入队的任务），区分等待 / 下载中 / 已完成 / 失败，进行中带进度条、实时速度、预计剩余时间与开始时间。
  - 上传页签记录"何时上传、传到哪个目录、大小、成败 / 秒传"，文件页上传与播放器「字幕传到云盘」两条入口共用。
- **外部 App 联动云下载**：支持 `mypad://download?url=…`、`pan115://…`、`magnet:` 链接，以及其它应用「分享到」纯文本；提交成败都会询问是否返回原来的应用。

### 修复

- **文件排序完全失效**：`/open/ufile/files` 的 `custom_order` 默认为 0，含义是"使用记忆排序、自定义排序失效"，此时 `o`/`asc` 会被服务端**完全忽略**，返回它自己记住的顺序 —— 表现为排序菜单怎么点列表都不变（而排序/视图的持久化本身是正常的，很容易误判成 UI 问题）。改为显式传 `custom_order=1`。
- **竖屏片旋转 90°/270° 后比例被拉伸**：`Modifier.size()` 会把请求尺寸收窄到父约束以内，旋转后按帧比例算出的布局尺寸是转置的（1080×2370 的竖屏片在 2560×1600 屏上需要 1167×2560），被父高夹住后布局比例从 0.4557 变成 0.7294，而 TextureView 是按布局框拉伸画面的，于是画面被横向压扁 1.6 倍。改用 `requiredSize`，布局比例恒等于片源比例。
- **播放器右上角规格标签不显示**：取样用 `snapshotFlow` 监听的是 `Player` 的非 Compose 状态属性，它只在首次求值一次（那一刻首帧还没解码，尺寸为 0×0），之后永不重发，胶囊整条渲染不出来。改用播放器监听器写入的 Compose 状态作为触发源。
- **下载直链 403**：115 的下载直链与"申请它时的 User-Agent"绑定，系统 DownloadManager 自带 UA 取文件一律 403。给下载请求显式加 UA，并与 API 客户端共用同一个常量，避免改一处漏一处又静默失败。顺带：打开文件按扩展名推断 MIME（DownloadManager 上报的常是 `application/octet-stream`，会把 apk 交给随机应用）；文件被外部删除时显示「文件已删除」并隐藏打开。
- **播放列表抽屉每次展开都回到顶部**：`LazyColumn` 的状态原本建在 `AnimatedVisibility` 内部，抽屉收起时内容被移出组合、状态随之销毁。已提升到抽屉之外并改用 `rememberSaveable`。同时切集（自动续播 / 上下集 / 随机 / 列表点播）会把当前集平滑滚到视口正中，用户正在拖动时先让位。
- **文件浏览切到"加载中 / 空目录 / 错误"分支后滚动位置丢失**：滚动状态声明在 `when` 分支内部，分支切换即被销毁，回到列表就从头开始。已提升到 `when` 之外，并改为**按目录分别记忆**位置（返回上级回到原处，进入内容更短的子目录不再把上级位置顶掉）。
- **大图被反复重复下载**：超大图的落盘缓存原先用 URL 的哈希命名，而 115 的原图直链带签名、每次列表响应都会变，于是同一张图每重新解析一次就被当成新文件重下一份（实测攒了 5 份字节数完全相同的文件、白占 103MB）。改用与签名无关的稳定标识（`pick_code`/`fid`）命名；同时新增 200MB 上限 + 按"最久未用"自动淘汰，原先这个目录**没有任何清理路径**。落盘也改为"先写临时文件再改名"，避免下载中断留下的残缺文件被当成有效缓存（那种坏文件永远不会重下，那张图就永久打不开）。设置页新增「图片 → 清除大图缓存」可手动回收。
- **移动 / 复制成功后毫无反馈**：成功结果被内部辅助函数无条件丢弃，而调用方只对"有内容"的结果弹提示，于是"已移动到「xxx」"成了死代码。现在移动与复制成功后会明确告知去向。

### 变更

- 移除文件对话框里的「复制直链」按钮 —— 改由传输中心承担，避免私密直链外泄。
- 外部 App 唤起优先于剪贴板内容探测，避免联动被剪贴板请求顶掉。
- 离线任务对话框：选定保存目录即落盘，下次手动 / 剪贴板 / 外部唤起默认仍存这里。

### 已知限制

- 文件夹置顶只保存在本机，换设备不同步（115 开放平台无置顶接口）。
- 大文件上传、BT 原生任务尚未实现。

## 0.1.0

首个公开版本。

- **登录**：扫码授权 / 免 AppID 导入 Token（access/refresh token 自动续期）
- **文件浏览**：列表 / 网格双视图、分类筛选、星标、搜索、面包屑导航、多选批量操作（移动 / 复制 / 重命名 / 星标 / 删除 / 下载）
- **图片浏览**：大图画廊（左右翻页、渐进式加载、相邻页预取、双击 / 捏合缩放、下拉关闭），超大图用 `BitmapRegionDecoder` 分块解码
- **视频播放器**：HLS 流式播放 + 本地缓存、手势（双击快进快退 / 左右滑调亮度音量 / 横滑非线性快进）、画面 90° 旋转、播放模式（顺序 / 单片循环 / 随机）、0.5x~4x 长按倍速、在线字幕搜索、字幕字号可调、播放列表抽屉、屏幕锁、多音轨、清晰度切换、右上角状态栏
- **云下载**：磁力链 / 电驴 / HTTP(S) / FTP 离线下载，.torrent 解析转磁力，保存位置持久化
- **回收站**：列表 / 还原 / 彻底删除 / 清空
- **文件上传**：小文件上传（≤32MB，断点续传协议已验证）
- **过滤规则**：条件树（文件名包含/排除/正则、大小、类型）+ 生效目录绑定，目录级就近继承
- **大屏适配**：手机单列 / 平板多列卡片流，宽屏内容居中防拉伸
